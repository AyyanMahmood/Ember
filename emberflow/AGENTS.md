# Agent Instructions

This project's agent/assistant instructions live in `CLAUDE.md` at the repository root. If your tool reads `AGENTS.md` by convention rather than `CLAUDE.md`, start there — it is the single source of truth for architecture rules, design system, and workflow for this project.
